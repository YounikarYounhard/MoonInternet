<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this only keeps the launch window dark so there is no white flash. -->
    <style name="Theme.Moon" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0D0A18</item>

        <!-- Edge to edge: the app paints under both bars, so the phone's own buttons sit on our
             night sky instead of a black strip. enforce*Contrast has to be off or Android
             re-adds a translucent scrim behind the navigation bar and undoes it. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
