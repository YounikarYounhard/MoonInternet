<?xml version="1.0" encoding="utf-8"?>
<!-- Without this file Android 8+ has only a legacy bitmap to work with: the launcher shrinks it
     into whatever mask the theme uses and paints its own backdrop behind it, which is what made
     the icon look soft and washed out on the phone. Here the art is the foreground on our own
     night-blue background, at the resolution the launcher actually wants. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/ic_launcher_background"/>
    <foreground android:drawable="@mipmap/ic_launcher_fg"/>
    <monochrome android:drawable="@mipmap/ic_launcher_fg"/>
</adaptive-icon>
